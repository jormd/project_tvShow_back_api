language: php

php:
  - '7.1'

env:
  - SYMFONY_VERSION="4.2.*"

## on test juste sur les branch dev et master
branches:
  only:
    - master
    - develop
    - deploymentTest

before-install:
  - composer self-update

install:
  - composer install

script:
  - php vendor/bin/phpunit
